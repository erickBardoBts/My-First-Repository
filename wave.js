const testInput = "word"
function wave(str){
    let waveStr = []
    let word = str.split('')
    for(let l = 0; l < waveStr.length; l++){
        word[i].toUpperCase
        waveStr.push(word)
    }

    return []
}

module.exports = wave;
